.env
.claude/
data/
syntheses/
proposals/
revisions/
models/
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/
.DS_Store

# Session JSON configs (config/sessions/*.json) ARE tracked in git going
# forward — they're managed via the admin UI and the user has chosen to
# version them. If a particular session contains community-sensitive
# content, gitignore it explicitly here.

# The Telegram binding is per-environment runtime state (which session
# the bot is currently routing to), not config worth versioning.
config/telegram.json
